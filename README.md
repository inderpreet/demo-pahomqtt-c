# MQTT, TLS Test and Demo

- I installed paho from repo. Build and install
- I built the mbedTLS from source as well BUT also installed from APT

# On a Raspberry Pi

```
sudo apt install libmbedtls-dev
```
